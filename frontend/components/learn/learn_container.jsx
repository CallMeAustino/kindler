import React from 'react';
import { connect } from 'react-redux';
import Learn from './learn';



const msp = (state) => {
    
    return {
    }
}

const mdp = (dispatch) => {
    return {
    }
}

export default connect(msp, mdp)(Learn);